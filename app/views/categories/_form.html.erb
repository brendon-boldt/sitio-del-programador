<%= form_for @category do |f| %>

  <% if @category.errors.any? %>
    <div id="error_exploration" %>
      <br>
      <b><%= pluralize(@category.errors.count, "error") %> prohibited this category from being created.
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

